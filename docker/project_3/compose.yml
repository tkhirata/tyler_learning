services:
  hello_app:
    build: ./hello_app
    ports:
      - "8080"

  proxy_app:
    build: ./proxy
    ports:
      - "8081"

  webserver:
    build: ./webserver
    ports:
     - "8082:80"

  nginx:
    build: ./nginx
    ports:
     - "8083:80"
